# TODO: Implement OTP Sending on User Registration

- [ ] Modify the `register` method in `src/auth/auth.service.ts` to automatically call `sendOtp` after successful user and wallet creation, and update the return message to indicate OTP has been sent.
